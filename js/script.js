// Utilizando LET
console.log("Antes, ", p);
//Para (Iniciar iterador ; iterador < total ; iterador++)
for (let p = 0; p < 3; p++) {
  console.log("- ", p);
}
console.log("Despues p :", p);

// Utilizando VAR
console.log("Antes, ", p);
for (var p = 0; p < 3; p++) {
  console.log("- ", p);
}
console.log("Despues p :", p);
